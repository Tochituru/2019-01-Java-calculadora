var numero1;
var numero2;
var suma = (numero1+numero2);
var resta = (numero1-numero2);
var multiplicacion = (numero1*numero2);
var division = (numero1/numero2);

parseInt(numero1);
parseInt(numero1);
parseInt(suma);
parseInt(resta);
parseInt(multiplicacion);
parseInt(division);


if (suma) {
    console.log(suma);
} else if (resta) {
    console.log(resta);
} else if (multiplicacion) {
    console.log (multiplicacion);
} else if (division) {
    console.log (division);
}

